using UnityEngine;
using System.Collections;

public class queen_sphere_functions : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
		// Initialize Queen piece
/*		Instantiate (queen_sphere,
		             new Vector3 (4, 4, 4),
		             Quaternion.identity);

*/	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
